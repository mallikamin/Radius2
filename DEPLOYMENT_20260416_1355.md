# Deployment Summary - 16 Apr 2026

## Commit
- **Hash**: 79d07f8
- **Message**: feat(vector): annotation UX improvements + per-plot font sizes
- **Branch**: master

## Changes Deployed

### 1. Annotation List Order (Issue #2) ✓
- Reversed annotation list in Sidebar - newest annotations now appear at top
- **File**: `frontend/src/components/Vector/Sidebar.jsx`

### 2. Color Picker for Add/Move (Issue #3) ✓
- Replaced hex code prompts with HTML5 color picker modals
- Applied to both "Add Annotation" and "Move to New" functions
- **Files**: `frontend/src/components/Vector/Sidebar.jsx`, `AnnotationEditor.jsx`

### 3. Per-Plot Font Size Control (Issue #4) ✓
- Added `plot_font_sizes` JSONB column to `vector_annotations` table
- UI: Per-plot font size toggle in AnnotationEditor
- Rendering: MapCanvas now checks per-plot sizes before falling back to global
- **Files**: 
  - Backend: `backend/app/main.py` (model + API)
  - Frontend: `AnnotationEditor.jsx`, `MapCanvas.jsx`, `useVectorState.js`
  - Database: Migration applied to production

### 4. Rotation Debug Logging (Issue #1) ⚠️
- Added console logging to track rotation save/load
- Logs: "SAVE DEBUG - Annotations with rotation" and "LOAD DEBUG - Annotations with rotation"
- **File**: `frontend/src/components/Vector/VectorMap.jsx`
- **Status**: Needs user testing to diagnose rotation loss issue

## Deployment Details

### Database Migration
```sql
ALTER TABLE vector_annotations 
ADD COLUMN IF NOT EXISTS plot_font_sizes JSONB DEFAULT '{}'::jsonb;
```
- **Status**: ✓ Applied to production DB (orbit_db)

### Frontend
- **Build**: `vite build` completed successfully
- **JS Hash**: `index-CfzVOPfs.js`
- **Uploaded**: All dist files to `~/orbit-crm/frontend/dist/`
- **Status**: ✓ Deployed

### Backend
- **File**: `main.py` updated with plot_font_sizes support
- **Container**: `orbit_api` rebuilt
- **Status**: ✓ Deployed

### Network & SSL
- POS network: orbit_api reconnected to `pos-system_default`
- Nginx: Reloaded successfully
- **Verification**:
  - HTTP Status: 200 ✓
  - SSL Subject: CN=orbit-voice.duckdns.org ✓

## Testing Instructions

1. **Test annotation order**: Add new annotation, verify it appears at top of list
2. **Test color picker**: Click "+ Add" button, use color picker instead of hex input
3. **Test per-plot fonts**:
   - Edit an annotation
   - Enable "Per-plot control" checkbox
   - Set different font sizes for different plots
   - Save and verify rendering on map
4. **Test rotation persistence**:
   - Open browser console (F12)
   - Edit annotation rotation
   - Save project (check "SAVE DEBUG" log)
   - Logout/refresh
   - Load project (check "LOAD DEBUG" log)
   - Report if rotation is preserved or lost

## Production Environment
- **Server**: 159.65.158.26
- **URL**: https://orbit-voice.duckdns.org/
- **Deployed**: 2026-04-16 08:47 UTC
